
name: Animal Farm NodeJS CI
on:
  push:
    branches:
      - "*"
jobs:
  build:
    runs-on: ubuntu-latest
    environment: demo
    steps:
    - name: Run tests
      run: echo "sample"
    - name: demo
      run: echo ${{ vars.TEST_OS_VERSION }}

  example_matrix:
    runs-on: ubuntu-latest
    needs: build
    environment: demo
    strategy:
      matrix:
        version: ${{ fromJSON(vars.TEST_OS_VERSION) }}
    steps:
    - name: devil
      run: echo "sample"